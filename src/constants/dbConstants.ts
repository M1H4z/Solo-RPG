// src/constants/dbConstants.ts

// Columns fetched for detailed Hunter object processing
export const HUNTER_BASE_COLUMNS = 
    'id, user_id, name, experience, rank, class, stat_points, skill_points, strength, agility, perception, intelligence, vitality, created_at, unlocked_skills, equipped_skills';

// Add other DB constants here if needed in the future 